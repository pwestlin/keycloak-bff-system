server:
  port: 8181

spring:
  application:
    name: backend-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000/realms/myrealm
          audiences:
            - backend-client-service
